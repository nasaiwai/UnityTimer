using UnityEngine;
using System.Collections;

public class Player : MonoBehaviour {

/*
	// Use this for initialization
	void Start () {
		CountDownTimer player1 = new CountDownTimer(100, 100, 200, 200);
		//CountDownTimer player2 = new CountDownTimer(300, 300, 200, 200);

		player1.OnGUI ();
		//CountDownTimer.countDownTimer.OnGUI ();
		//player2.OnGUI ();
	}

	void OnGUI() {
		if (player1.timeRemaining > 0) {
			if (player1.seconds >= 10) {
				GUI.Label (new Rect (200, 200, 100, 100), "Time Remaining : " + base.minutes + ":" + base.seconds);
			} 
			else {
				GUI.Label (new Rect (200, 200, 100, 100), "Time Remaining : " + base.minutes + ":0" + base.seconds);
			}
		} 
		else {
			GUI.Label (new Rect (200, 200, 100, 100), "Time's up");
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
}
